git status                     # xem thay đổi
git add -A                     # thêm tất cả file đã đổi
git commit -m "Mô tả ngắn gọn" # tạo commit
git pull --rebase origin main  # kéo về trước để tránh lệch lịch sử
git push origin main           # đẩy lên remote

psql "postgresql://postgres:V!etdu1492003@localhost:5432/analytics"


#update code from github
git remote -v                 # kiểm tra URL remote
git branch -vv                # xem đang ở nhánh nào và tracking gì
git fetch origin --prune      # lấy về cập nhật mới nhất

git fetch origin
git reset --hard origin/main   # đưa working tree/HEAD giống hệt GitHub
git clean -fd                  # xóa file/thư mục chưa theo dõi (untracked)
# (không dùng -x trừ khi muốn xóa cả file bị .gitignore)
